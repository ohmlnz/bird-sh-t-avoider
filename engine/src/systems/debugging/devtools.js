/** 
*  component that can be used to display useful debugging info such as:
*  - nb of birds/turds in state to check for proper gargbage collection
*  - current physics engine settings (gravity, velocity, friction, etc.)
*  - position of elements
*  - etc ... 
**/

export default (entities, { dispatch }) => {
  // find out how to display score on screen
  //console.log(entities.score)
  return entities;
}
